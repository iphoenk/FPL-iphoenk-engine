name: V4.9.7 comparator integration correction

on:
  push:
    branches:
      - codex/v4.9.7-challenger-comparator

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/v4.9.7-challenger-comparator
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tools/v497_fix_comparator.py
      - name: Commit correction if needed
        shell: bash
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/services/challenger_comparator_service.py config/architecture_ownership_registry.json
          git commit -m "fix(v4): correct comparator trend ownership"
          git push origin HEAD:codex/v4.9.7-challenger-comparator
