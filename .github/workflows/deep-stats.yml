name: FPL manual deep-stats compatibility run

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deep:
    uses: ./.github/workflows/fpl-engine-core.yml
    permissions:
      contents: write
    with:
      ref: v4-prediction-engine
      publish: true
