name: FPL V4 production gate

on:
  workflow_dispatch:
  schedule:
    # GitHub cron is UTC. Minute :30 is also minute :30 in WIB (UTC+7),
    # so this supplies the Master Monitor hourly evaluation checkpoint.
    - cron: "30 * * * *"
  push:
    branches: [v4-prediction-engine]
    paths-ignore: ['data/**']
  pull_request:
    branches: [v4-prediction-engine]

permissions:
  contents: write

concurrency:
  group: fpl-iphoenk-v4-gate
  cancel-in-progress: true

jobs:
  core:
    uses: ./.github/workflows/fpl-engine-core.yml
    permissions:
      contents: write
    with:
      ref: ${{ github.event_name == 'schedule' && 'v4-prediction-engine' || github.sha }}
      publish: ${{ github.event_name != 'pull_request' }}
