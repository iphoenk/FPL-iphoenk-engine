name: FPL iphoenk collector v3.21.0 microservices

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Legacy workflow notice
        run: |
          echo "The monolithic V3 workflow is retired."
          echo "Use v3-ci.yml, v3-runtime-fast.yml, or v3-refresh-full.yml."
